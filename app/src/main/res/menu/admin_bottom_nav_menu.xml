<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Home Button (Same as User) -->
    <item
        android:id="@+id/nav_home"
        android:icon="@drawable/ic_home"
        android:title="Home" />

    <!-- Add Notice Button (Using the user's 'add post' icon) -->
    <item
        android:id="@+id/nav_add_notice"
        android:icon="@drawable/ic_add_circle"
        android:title="Add Notice" />

    <!-- Duplicates Button (Using the user's 'alerts' icon) -->
    <item
        android:id="@+id/nav_duplicates"
        android:icon="@drawable/ic_alert"
        android:title="Duplicates" />

    <!-- Profile Button (Same as User) -->
    <item
        android:id="@+id/nav_profile"
        android:icon="@drawable/ic_person"
        android:title="Profile" />

</menu>
